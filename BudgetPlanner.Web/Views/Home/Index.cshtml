@model HomeViewModel

@{
    ViewBag.Title = "Dashboard";
}

<budget-dashboard request-url="@Url.Action("GetBudgetPlanners","Budget")"
                  details-url="@Url.Action("Details", "Budget", new { reference = "!ref" })"
                  :last-updated='new Date("@Model.LastUpdated.MillisecondsSinceUnixEpoch()")'
                  v-on:item:save="setModal">

</budget-dashboard>

@Html.Raw(Model.Content)