@model HomeViewModel

@{ ViewBag.Title = "Dashboard"; }

<section data-src="@Url.Action("BudgetPlannerListDashboard","ViewComponent")"
         data-parameters="@Html.Base64Encode(Json.Serialize(new BudgetPanelDashboardListViewModel { LastUpdated = Model.LastUpdated }), Encoding.UTF8)">
    <div class="spinner-border" role="status">
        <span class="sr-only">Loading...</span>
    </div>
</section>